import {NgModule} from '@angular/core';
import {MenuService} from './menu.service';

@NgModule({
    declarations: [],
    imports: [ ],
    providers: [
        MenuService
    ]
})

export class MenuModule {
}
